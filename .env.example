DATABASE_URL="file:./dev.db"

# Optional — activates the real AI layer (src/lib/ai/). Without these the app
# runs entirely on the phase-1 heuristics and works fine.
# ANTHROPIC_API_KEY=
# VOYAGE_API_KEY=
